name: Build and Publish Docker Image (Main)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  docker-main:
    name: Publish main image to GitHub registry
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: main

      - name: Login to GitHub Container Registry
        run: echo ${{ github.token }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build docker image
        run: docker build -t ghcr.io/gildedroach/metrics:main .

      - name: Publish to GitHub Container Registry
        run: docker push ghcr.io/gildedroach/metrics:main

      - name: Tag as branch name
        run: |
          docker tag ghcr.io/gildedroach/metrics:main ghcr.io/gildedroach/metrics:main
          docker push ghcr.io/gildedroach/metrics:main

      - name: Summary
        run: |
          echo "✅ Docker image published successfully!"
          echo ""
          echo "**Image tags:**"
          echo "- \`ghcr.io/gildedroach/metrics:main\`"
          echo ""
          echo "**Usage in GitHub Actions:**"
          echo "\`\`\`yaml"
          echo "uses: docker://ghcr.io/gildedroach/metrics:main"
          echo "\`\`\`"
